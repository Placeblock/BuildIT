#version 430

layout (std430, set = 0, binding = 2) writeonly buffer cmd {
    uint vertexCount;
    uint instanceCount;
    uint firstVertex;
    uint firstInstance;
};

void main() {
    vertexCount = 6;
    instanceCount = 0;
    firstVertex = 0;
    firstInstance = 0;
}