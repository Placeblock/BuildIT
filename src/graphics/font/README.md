The font directory contains code for loading and rendering text

It loads bitmap font files and their metadata file respectively.
It generates a texture and uploads it to the gpu and stores the metadata
into structs.